<br>
<br>
<br>
<br>


Create a list and get busy:

<%= form_for @list, url: {action: "create"} do | f | %>
  
  <%= f.label :name %>:<br>
  <%= f.text_field :name %>
  <%= hidden_field_tag :id, @current_user.id %>
  <br>


  <%= submit_tag("Create list") %>

<% end %>
